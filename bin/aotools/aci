#!/bin/ksh

# Arch group source code check in
# Uses scmci to do the work.

PATH=/bbsrc/bin/prod/bin/robo:$PATH

scmci "$@"

i=

for arg in "$@"
do
    i="$arg"
done

i=${i##*/}
i=${i%.*}

/home/dsugalsk/src/hold_pg_refresh_db.pl performance $i 2>/dev/null >/dev/null &
